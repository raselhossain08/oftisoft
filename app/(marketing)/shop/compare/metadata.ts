import type { Metadata } from "next";
import { constructMetadata } from "@/lib/metadata";

export const metadata: Metadata = constructMetadata({
  title: "Compare Products | Side-by-Side Comparison",
  description: "Compare features, pricing, and specifications of our products side by side to make the best choice for your project.",
  keywords: [
    "product comparison",
    "compare",
    "side by side",
    "feature comparison",
    "compare products",
  ],
});
